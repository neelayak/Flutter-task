class SearchDatamodel {
  String objectID;
  String title;
  int points;
  String text;

  SearchDatamodel.searchscreen(this.objectID, this.title, this.points);

  SearchDatamodel.populate(this.text);
}
